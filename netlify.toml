second change as per co-pilot to resolve the build error in netlify.
# No build needed - Jenkins handles building
# This config is for serving the pre-built artifact from CI/CD

[[redirects]]
from = "/*"
to = "/index.html"
status = 200
